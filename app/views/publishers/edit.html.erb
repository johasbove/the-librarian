<h1>Editing publisher</h1>

<%= render 'form' %>

<%= link_to 'Show', @publisher, html_options = {class: "btn btn-primary"} %> |
<%= link_to 'Back', publishers_path, html_options = {class: "btn btn-default"} %>
